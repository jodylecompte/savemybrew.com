import { Layout } from "@/components/global";

const pageMeta = {
  title: "Brewing Resources",
  description: "TODO: Write meta descriptions",
};

const ResourcesPage = () => {
  return (
    <Layout meta={pageMeta}>
      <div className="m-auto max-w-7xl">Resources Page</div>
    </Layout>
  );
};

export default ResourcesPage;
